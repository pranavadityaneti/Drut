{
    "name": "drut-monorepo",
    "private": true,
    "scripts": {
        "dev:web": "npm run dev --workspace=@drut/web",
        "build:web": "npm run check:labels && npm run build --workspace=@drut/web",
        "check:labels": "node scripts/check-no-framework-labels.mjs",
        "lint": "npm run check:labels && npm run lint --workspaces"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "overrides": {
        "react": "19.2.3",
        "react-dom": "19.2.3"
    }
}