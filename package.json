{
    "name": "drut-monorepo",
    "private": true,
    "scripts": {
        "dev:web": "npm run dev --workspace=@drut/web",
        "build:web": "npm run build --workspace=@drut/web",
        "lint": "npm run lint --workspaces"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "overrides": {
        "react": "19.1.0",
        "react-native-screens": "4.16.0"
    }
}