# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables
.env
.env.local
.env.*.local
.vercel
.env*.local
# Calibration / scripts secrets (never commit)
.env.calibration
scripts/.env.calibration

# Local script artifacts / temp outputs (not committed)
scripts/*.json
scripts/.backup-*
scripts/.canonical-topics.txt
scripts/.pyq-pages/
packages/shared/src/lib/.taxonomy.ts.bak

# Agent / worktree / editor local state
.claude/
.worktrees/
.stale-from-main-checkout/
*.code-workspace
apps/web/supabase/.temp/

# Copyrighted textbook PDFs — NEVER commit (legal). Source material is local-only.
docs/question-generation/textbooks/

# OpenAI usage ledger raw data (runtime artifact; the HTML report is the deliverable).
scripts/.openai-usage.jsonl
